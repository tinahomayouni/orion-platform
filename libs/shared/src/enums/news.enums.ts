export enum NewsSource {
  FOREXFACTORY = 'forexfactory',
  INVESTOR = 'investor',
}

export enum SourceImpact {
  HIGH = 'high',
  MEDIUM = 'medium',
  LOW = 'low',
  UNKNOWN = 'unknown',
}

export enum ProcessingStatus {
  COLLECTED = 'collected',
  SCORED = 'scored',
  ANALYZED = 'analyzed',
}
